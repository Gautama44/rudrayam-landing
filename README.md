# NexVoraSystem — React (Vite) version

Steps to run locally:

1. Install dependencies

```bash
npm install
```

2. Start dev server

```bash
npm run dev
```

This project was scaffolded from an existing static HTML/CSS site and converted to a minimal React + Vite setup.
